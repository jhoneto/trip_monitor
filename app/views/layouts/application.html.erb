<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Trip Monitor" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Leaflet CSS %>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" 
          integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="" />
    
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <%# Leaflet JavaScript %>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" 
            integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin=""></script>
  </head>

  <body class="bg-gray-100">
    <div class="min-h-screen flex">
      <!-- Sidebar -->
      <div class="bg-gray-800 text-white w-64 min-h-screen p-4">
        <div class="mb-8">
          <h2 class="text-xl font-bold">Trip Monitor</h2>
        </div>
        
        <nav class="space-y-2">
          <%= link_to "Dashboard", root_path, class: "block py-2 px-4 rounded hover:bg-gray-700 transition-colors" %>
          <%= link_to "Viagens", kitetrips_path, class: "block py-2 px-4 rounded hover:bg-gray-700 transition-colors" %>
          <%= link_to "Empresas", companies_path, class: "block py-2 px-4 rounded hover:bg-gray-700 transition-colors" %>
        </nav>
        
        <div class="mt-auto pt-8">
          <div class="border-t border-gray-700 pt-4">
            <% if user_signed_in? %>
              <div class="flex items-center space-x-3">
                <div class="w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center text-white text-sm font-bold">
                  <%= current_user.email.first.upcase %>
                </div>
                <div class="flex-1 min-w-0">
                  <p class="text-sm font-medium text-white truncate">
                    <%= current_user.email %>
                  </p>
                </div>
              </div>
              <%= link_to "Sair", destroy_user_session_path, 
                  method: :delete, 
                  class: "block w-full text-left py-2 px-4 mt-2 rounded hover:bg-gray-700 transition-colors text-red-300" %>
            <% else %>
              <%= link_to "Entrar", new_user_session_path, class: "block py-2 px-4 rounded hover:bg-gray-700 transition-colors text-blue-300" %>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Main content area -->
      <div class="flex-1 flex flex-col">
        <!-- Header -->
        <header class="bg-white shadow-sm border-b border-gray-200 p-4">
          <div class="flex items-center justify-between">
            <h1 class="text-2xl font-semibold text-gray-800">
              <%= content_for?(:page_title) ? yield(:page_title) : "Dashboard" %>
            </h1>
            
            <div class="flex items-center space-x-4">
              <% if user_signed_in? %>
                <span class="text-sm text-gray-600">Bem-vindo, <%= current_user.email %></span>
              <% end %>
            </div>
          </div>
        </header>

        <!-- Flash messages -->
        <% if notice %>
          <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded m-4">
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded m-4">
            <%= alert %>
          </div>
        <% end %>

        <!-- Page content -->
        <main class="flex-1 p-6">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
